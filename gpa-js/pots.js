var myGrades  = [ 96, 92, 86 ];
var myLetters = [ 'A', 'A-', 'B' ];
var myCredits = [ 3, 4, 2 ];

/*
 * How do I logically figure out my gpa for this group of data?
 *  What would the algorithm be?    pseudocode
 *
 */

var school = require('./school.js');

var object = school.course;

console.log(object);
